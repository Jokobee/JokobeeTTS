name: Publish Free (core + free + aliases) to Maven Central

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to publish (e.g. 1.1.0) — must match coordinates() in core/free build.gradle.kts.'
        required: true

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Skip if version already on Maven Central
        id: check
        run: |
          VERSION="${{ github.event.inputs.version }}"
          URL="https://repo1.maven.org/maven2/com/jokobee/jokobeetts/${VERSION}/jokobeetts-${VERSION}.pom"
          if curl -sf "$URL" > /dev/null 2>&1; then
            echo "Version ${VERSION} already published — aborting."
            exit 1
          fi

      - name: Publish core + free + aliases to Maven Central
        env:
          ORG_GRADLE_PROJECT_mavenCentralUsername: ${{ secrets.OSSRH_USERNAME }}
          ORG_GRADLE_PROJECT_mavenCentralPassword: ${{ secrets.OSSRH_PASSWORD }}
          ORG_GRADLE_PROJECT_signingInMemoryKey: ${{ secrets.GPG_PRIVATE_KEY }}
          ORG_GRADLE_PROJECT_signingInMemoryKeyPassword: ${{ secrets.GPG_PASSPHRASE }}
        run: |
          chmod +x ./gradlew
          ./gradlew :core:publishToMavenCentral :free:publishToMavenCentral \
            :tts-ai-android:publishToMavenCentral :tts-android-ai:publishToMavenCentral \
            --no-configuration-cache --no-daemon
          # kokoro-french-voice-female / kokoro-female-french-voice / kokoro-french-voice-marine
          # were a ONE-TIME publish (fixed version 1.0.0, already live on Maven Central) —
          # deliberately NOT re-included here. Re-running them would resubmit an identical
          # component and fail the whole batch deployment ("already exists").
