package com.jokobee.tts.core

/**
 * :core — contrats partagés (Free et Pro en dépendent).
 *
 * À porter ici (voir SESSION-FINAL.md §1) :
 *  - `TextSplitter`         ← text_splitter.py
 *  - `LangRouter` (interface) + `ExplicitLangRouter`
 *  - `Voice`, `VoiceError`, exceptions et contrats
 *
 * Placeholder — aucun code métier pour l'instant (squelette de build P0).
 */
public object JokobeeTtsCore {
    public const val VERSION: String = "0.1.0"
}
